# 运行验收记录

## 1. 验收环境

| 项目 | 内容 |
| --- | --- |
| 版本 | T5 静态基线 / 2026-08-01 |
| 本地启动方式 | `npm run dev -- --host 127.0.0.1 --port 5173` |
| 本地预览 | `http://127.0.0.1:5173/` |
| 目标视口 | 参会人员 `390 x 844`；会务审核 `1440 x 900`；中等宽度在 `<1024px` 切换为单主列 |
| 验收时间 | 2026-08-01 |
| 验收范围 | T5 静态布局、关键场景直达入口、视觉令牌与响应式基础。不含 T6 业务动作。 |

## 2. 静态场景验证

| 状态 ID | 访问方式 | 前置数据 | 预期结果 | 实际结果 | 证据 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| MEET-01 | `?screen=discover&scenario=seed` | 预置会议 | 会议发现、推荐卡、筛选外观与底部导航可见。 | 通过 | 浏览器截图与语义快照 | 静态通过 |
| BOOK-01 | `?screen=conflict&scenario=seed` | 预置冲突 | 预约摘要、冲突理由、AI 建议与后续动作层级可见。 | 通过 | 直达入口加载 | 静态通过 |
| REVIEW-01 | `?screen=review&scenario=pending` | 预置待审项 | 侧栏、审核队列、证据与决策区完整可见；`1280px` 无横向溢出。 | 通过 | 浏览器截图、宽度/滚动宽度均为 `1280px` | 静态通过 |
| SCHEDULE-01 | `?screen=schedule&scenario=approved` | 已批准预约 | 日期轨道、当地时区时间线、变更提醒与凭证可见。 | 通过 | 直达入口加载 | 静态通过 |
| CHECKIN-01 | `?screen=checkin&scenario=approved` | 预置模拟码 | 核验码输入、预置成功结果与最少必要信息可见。 | 通过 | 直达入口加载 | 静态通过 |
| ADMIN-01 | `?screen=admin&scenario=seed` | 本地记录 | 角色边界与可追溯操作记录可见。 | 通过 | 直达入口加载 | 静态通过 |

## 3. 构建与质量检查

- [x] `npm run build` 通过，TypeScript 与 Vite 均无编译错误。
- [x] 所有关键静态场景均可从 URL 参数直接加载。
- [x] 会议发现页已核验白底会议卡、`1.5px` 渐变细边框、紫色选中态和底部导航。
- [x] 会务审核页已核验三栏布局与 `<1024px` 单主列响应式规则。
- [x] 已保留键盘跳转主内容入口、可见焦点样式、语义按钮/链接及 `44px` 左右的图标触控区域。
- [ ] T6 业务动作、持久化、输入校验、加载/空态/错误恢复和权限守卫尚未实现。

## 4. 已知限制与阶段结论

- 静态状态：当前所有提交、批准、调整、核验等写入动作均为禁用展示，避免伪造已实现交互。
- 运行边界：无外部网络、服务端、真实账户、真实二维码或真实 AI 调用。
- 阶段结论：`待用户确认`。确认后进入 T6，按 T1/T2 规则接通领域动作与同步状态。
